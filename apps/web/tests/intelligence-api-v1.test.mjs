import test from "node:test";import assert from "node:assert/strict";import {readFileSync,existsSync} from "node:fs";
const root=new URL("../src/app/api/v1/",import.meta.url);const routes=["status/route.ts","sources/route.ts","events/route.ts","events/[eventId]/route.ts","timeline/route.ts","breaking/route.ts","radar/route.ts","map/route.ts","risk/route.ts","reports/route.ts","search/route.ts","entities/route.ts","entities/[entityId]/route.ts","entities/[entityId]/relationships/route.ts","graph/route.ts"];
test("all documented v1 public routes exist and no mutation families are exposed",()=>{for(const route of routes)assert.equal(existsSync(new URL(route,root)),true,route);for(const excluded of ["watchlists","alerts","notifications","test-webhook"])assert.equal(existsSync(new URL(`${excluded}/route.ts`,root)),false,excluded)});
test("v1 adapter uses standard envelope and request ID",()=>{const source=readFileSync(new URL("../src/lib/api/v1-api.ts",import.meta.url),"utf8");assert.match(source,/apiVersion:API_VERSION/);assert.match(source,/X-Request-Id/);assert.match(source,/retryable/);assert.match(source,/pageSize>100/) });
test("OpenAPI documents the implemented v1 namespace",()=>{const spec=readFileSync(new URL("../../../docs/openapi/atlas-intelligence-api-v1.yaml",import.meta.url),"utf8");assert.match(spec,/openapi: 3\.0/);assert.match(spec,/\/api\/v1\/status/);assert.doesNotMatch(spec,/\/api\/v1\/watchlists/)});
