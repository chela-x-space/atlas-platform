import type { AtlasEventCategory,AtlasEventQuery,AtlasEventSort,AtlasSeverity } from "@/types/atlas-data";
const categories=new Set<AtlasEventCategory>(["earthquake","cyclone","weather","space","technology","news","health","wildfire","flood","volcano","conflict","aviation","marine","market","cyber","energy","unknown"]),severities=new Set<AtlasSeverity>(["info","low","moderate","high","critical","unknown"]),sorts=new Set<AtlasEventSort>(["occurredAt:desc","occurredAt:asc","updatedAt:desc","severity:desc"]);
export function parseEventQuery(params:URLSearchParams):{ok:true;query:AtlasEventQuery}|{ok:false;message:string}{const list=(k:string)=>params.getAll(k).flatMap(v=>v.split(",")).filter(Boolean);const cs=list("category"),ss=list("severity"),sources=list("source");if(cs.some(v=>!categories.has(v as AtlasEventCategory)))return{ok:false,message:"Invalid category"};if(ss.some(v=>!severities.has(v as AtlasSeverity)))return{ok:false,message:"Invalid severity"};const number=(k:string,min:number,max:number)=>{const raw=params.get(k);if(raw===null)return undefined;const n=Number(raw);return Number.isFinite(n)&&n>=min&&n<=max?n:null;};const lat=number("latitude",-90,90),lon=number("longitude",-180,180),radius=number("radiusKm",0,20_000),limit=number("limit",1,500);if(lat===null||lon===null||radius===null||limit===null)return{ok:false,message:"Invalid numeric parameter"};if([lat,lon,radius].filter(v=>v!==undefined).length!==0&&[lat,lon,radius].some(v=>v===undefined))return{ok:false,message:"latitude, longitude and radiusKm must be supplied together"};const after=params.get("after")??undefined,before=params.get("before")??undefined;if((after&&!Number.isFinite(Date.parse(after)))||(before&&!Number.isFinite(Date.parse(before))))return{ok:false,message:"Invalid date parameter"};const sort=params.get("sort")??undefined;if(sort&&!sorts.has(sort as AtlasEventSort))return{ok:false,message:"Invalid sort"};const search=params.get("search")??undefined;if(search&&search.length>200)return{ok:false,message:"Search is too long"};return{ok:true,query:{...(cs.length?{categories:cs as AtlasEventCategory[]}:{}),...(ss.length?{severities:ss as AtlasSeverity[]}:{}),...(sources.length?{sourceIds:sources}:{}),...(search?{search}:{}),...(after?{occurredAfter:new Date(after).toISOString()}:{}),...(before?{occurredBefore:new Date(before).toISOString()}:{}),...(lat!==undefined?{latitude:lat!}:{}),...(lon!==undefined?{longitude:lon!}:{}),...(radius!==undefined?{radiusKilometers:radius!}:{}),...(limit!==undefined?{limit:limit!}:{}),...(params.get("cursor")?{cursor:params.get("cursor")!}:{}),...(sort?{sort:sort as AtlasEventSort}:{})}};}
