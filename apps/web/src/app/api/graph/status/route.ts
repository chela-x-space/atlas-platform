import {NextResponse} from "next/server";import {getEntityGraph} from "@/lib/graph/entity-graph-service";export const dynamic="force-dynamic";
export async function GET(){try{const graph=await getEntityGraph();return NextResponse.json(graph.status,{status:graph.status.degradedModules.length?206:200,headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300"}})}catch{return NextResponse.json({ready:false,nodeCount:0,edgeCount:0,entityCount:0,nodeTypesIncluded:[],relationshipTypesIncluded:[],generatedAt:null,degradedModules:["entity-graph"],warnings:["Canonical entity graph is unavailable."]},{status:503})}}
