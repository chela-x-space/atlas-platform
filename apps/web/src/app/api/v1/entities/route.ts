import {NextRequest} from "next/server";import {parseEntityQuery} from "@/lib/graph/entity-graph-query.mjs";import {getEntities} from "@/lib/graph/entity-graph-service";import {ok,fail} from "@/lib/api/v1-api";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const parsed=parseEntityQuery(request.nextUrl.searchParams);if(!parsed.ok)return fail(request,parsed.code,parsed.message);try{const value=await getEntities(parsed.filters);return ok(request,{entities:value.entities,facets:value.facets,totalEntities:value.totalEntities,totalPages:value.totalPages},{generatedAt:value.generatedAt,degraded:value.degraded,pagination:{strategy:"page",page:value.page,pageSize:value.pageSize,totalEntities:value.totalEntities,totalPages:value.totalPages},status:value.degraded?206:200})}catch{return fail(request,"ENTITIES_UNAVAILABLE","Canonical entities are unavailable",503)}}
