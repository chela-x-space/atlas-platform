import {NextRequest} from "next/server";import {getTimeline} from "@/lib/timeline/timeline-service";import {parseTimelineQuery} from "@/lib/timeline/timeline-logic.mjs";import {ok,fail} from "@/lib/api/v1-api";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const parsed=parseTimelineQuery(request.nextUrl.searchParams);if(!parsed.ok)return fail(request,parsed.code,parsed.message);try{const result=await getTimeline(parsed.filters);if(!result.ok)return fail(request,result.code,result.message);return ok(request,result.response,{generatedAt:result.response.fetchedAt,degraded:Boolean(result.response.partial),pagination:{strategy:"cursor",nextCursor:result.response.nextCursor},status:result.response.partial?206:200})}catch{return fail(request,"TIMELINE_UNAVAILABLE","The global timeline is unavailable",503)}}
