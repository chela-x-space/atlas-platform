import {NextRequest,NextResponse} from "next/server";import {parseSearchQuery} from "@/lib/search/search-engine.mjs";import {executeSearch} from "@/lib/search/search-service";export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const parsed=parseSearchQuery(request.nextUrl.searchParams);if(!parsed.ok)return NextResponse.json({error:{code:parsed.code,message:parsed.message}},{status:400});try{const value=await executeSearch({...parsed.filters,page:1,pageSize:1});return NextResponse.json({generatedAt:value.generatedAt,facets:value.facets,totalResults:value.totalResults,degraded:value.degraded,warnings:value.warnings},{status:value.degraded?206:200})}catch{return NextResponse.json({error:{code:"SEARCH_FACETS_UNAVAILABLE",message:"Contextual facets are unavailable"}},{status:503})}}
