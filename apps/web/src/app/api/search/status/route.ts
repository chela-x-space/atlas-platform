import {NextResponse} from "next/server";import {getSearchIndex} from "@/lib/search/search-service";export const dynamic="force-dynamic";
export async function GET(){try{const index=await getSearchIndex();return NextResponse.json(index.status,{status:index.status.degradedModules.length?206:200,headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300"}})}catch{return NextResponse.json({ready:false,documentCount:0,contentTypesIndexed:[],generatedAt:null,degradedModules:["search-index"],warnings:["Canonical search index is unavailable."]},{status:503,headers:{"Cache-Control":"no-store"}})}}
