import {NextRequest,NextResponse} from "next/server";import {parseEntityQuery} from "@/lib/graph/entity-graph-query.mjs";import {getEntities} from "@/lib/graph/entity-graph-service";export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const parsed=parseEntityQuery(request.nextUrl.searchParams);if(!parsed.ok)return NextResponse.json({error:{code:parsed.code,message:parsed.message}},{status:400});try{const value=await getEntities(parsed.filters);return NextResponse.json(value,{status:value.degraded?206:200,headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300"}})}catch{return NextResponse.json({error:{code:"ENTITIES_UNAVAILABLE",message:"Canonical entity projection is unavailable; no replacement entities were generated"}},{status:503})}}
