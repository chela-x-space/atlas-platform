import {NextRequest,NextResponse} from "next/server";import {parseEntityQuery} from "@/lib/graph/entity-graph-query.mjs";import {getEntityRelationships} from "@/lib/graph/entity-graph-service";export const dynamic="force-dynamic";
export async function GET(request:NextRequest,{params}:{params:Promise<{entityId:string}>}){const parsed=parseEntityQuery(request.nextUrl.searchParams);if(!parsed.ok)return NextResponse.json({error:{code:parsed.code,message:parsed.message}},{status:400});const {entityId}=await params;try{const value=await getEntityRelationships(entityId,parsed.filters);return value?NextResponse.json(value,{status:value.degraded?206:200}):NextResponse.json({error:{code:"ENTITY_NOT_FOUND",message:"Canonical entity was not found"}},{status:404})}catch{return NextResponse.json({error:{code:"ENTITY_RELATIONSHIPS_UNAVAILABLE",message:"Canonical relationships are unavailable"}},{status:503})}}
