import {NextResponse} from "next/server";import {getEntity} from "@/lib/graph/entity-graph-service";export const dynamic="force-dynamic";
export async function GET(_request:Request,{params}:{params:Promise<{entityId:string}>}){const {entityId}=await params;try{const value=await getEntity(entityId);return value?NextResponse.json(value,{status:value.degraded?206:200}):NextResponse.json({error:{code:"ENTITY_NOT_FOUND",message:"Canonical entity was not found"}},{status:404})}catch{return NextResponse.json({error:{code:"ENTITY_UNAVAILABLE",message:"Canonical entity graph is unavailable"}},{status:503})}}
