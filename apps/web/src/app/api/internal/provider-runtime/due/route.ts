import type { NextRequest } from "next/server";import { authorizeProviderRuntime,runtimeApiError,runtimeOk } from "@/lib/provider-runtime/internal-provider-runtime-api";import { getProviderRuntime } from "@/lib/provider-runtime/provider-runtime-service";
export const dynamic="force-dynamic";export async function GET(request:NextRequest){const denied=authorizeProviderRuntime(request);if(denied)return denied;try{return runtimeOk({due:await getProviderRuntime().due()})}catch(error){return runtimeApiError(error)}}
