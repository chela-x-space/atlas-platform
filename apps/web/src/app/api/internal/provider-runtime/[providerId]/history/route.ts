import type { NextRequest } from "next/server";import { authorizeProviderRuntime,runtimeApiError,runtimeOk } from "@/lib/provider-runtime/internal-provider-runtime-api";import { getProviderRuntime } from "@/lib/provider-runtime/provider-runtime-service";
export const dynamic="force-dynamic";type Context={params:Promise<{providerId:string}>};export async function GET(request:NextRequest,{params}:Context){const denied=authorizeProviderRuntime(request);if(denied)return denied;try{return runtimeOk(await getProviderRuntime().history(decodeURIComponent((await params).providerId)))}catch(error){return runtimeApiError(error)}}
